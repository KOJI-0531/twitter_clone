<div class="main user-show">
  <div class="container">
    <div class="user">
      <!--<h2><%=@user.name %></h2>-->
      <h2><%=@user.email %></h2>
      <% @user.posts.each do |post| %>
      <%= link_to(post.content, "/posts/#{post.id}") %>
      <% end %>
      
      
    </div>
  </div>
</div>